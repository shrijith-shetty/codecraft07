function add(a: number, b: number): number {
  return a + b;
}

function carSpeed(distance: number, velocity: number): number {
  return distance * velocity;
}

function suib(a: number, b: number): number {
  return a + b;
}

//hi my new branch
//branch name is spaceship
